<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.jasaharian">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme" tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".views.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"
                        android:theme="@style/SplashTheme" />

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".views.home.HomeActivity" />
        <activity android:name=".views.auth.signIn.SignInActivity" android:theme="@style/AuthTheme"/>
        <activity android:name=".views.auth.register.RegisterActivity" android:theme="@style/AuthTheme"/>
        <activity android:name=".views.auth.forgotPassword.ForgotPasswordActivity" android:theme="@style/AuthTheme"/>
        <activity android:name=".views.auth.forgotPassword.ResetPasswordActivity" android:theme="@style/AuthTheme"/>
    </application>

</manifest>